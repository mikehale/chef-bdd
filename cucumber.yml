default: features -r features -e data/*
focus: features -r features -t focus
autotest: features -r features -e data/* --color --format pretty
autotest-all: features -r features -e data/* --color --format progress